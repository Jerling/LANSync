# LANSync 项目记忆

## 团队 Profile
- android-dev：修复开发
- reviewer：Code review（Constructive feedback, correctness/security/maintainability/performance）
- qa：测试验证
- pm：项目管理
- ops：版本发布（commit + push）

## 团队通用流程
### 策划阶段（项目准入）
1. architect 提出方案
2. coder + reviewer + qa 各自独立审阅
3. 三方共同讨论 → 与 architect 达成四方一致
   - **有评审项不通过时 → 打回 architect 补充/重新设计 → 重新走审阅流程**
   - 循环此步直至所有项通过，或团队决定接受有保留意见的项（需明确记录）
4. pm 主持撰写立项 PPT（其他人辅助提供内容）
5. 用户评审价值 → 通过则立项，不通过则重新策划或终止

### 开发阶段（功能迭代）
1. android-dev/coder 开发
2. reviewer code review
3. qa 测试验证
4. pm 确认
5. ops 编译部署安装本地 commit
6. 用户验证通过
7. ops push 远程
> qa 不通过 → 直接打回开发者重测（不重经 reviewer），打回从 qa 重新走

### Bug 修复流程
发现 bug → 反馈 → kanban 建任务 → 走开发阶段修复（立项项目 or 结项后均适用）

## 项目结构
- App/：Android Compose App（70%）
- Server/：Flask 后端（95%）
- Web/：Vue3 PWA 前端
- `git remote`：gitcode = Jerling/LANsync，gitee = Jerling/LANSync

## 已知 Bug
- #10：同步死循环（每次扫到2个文件重复同步）→ **已修复，待 review**
- #11：同步删除本地文件功能失效 → **已修复，待 review**

## 待提交改动（6次 commit 方案已确认）
- Commit 1：MainActivity.kt 清理 DiCar 代码
- Commit 2：SyncManager.kt / SyncStateHolder.kt 新增
- Commit 3：GalleryScreen.kt / GalleryViewModel.kt（Fix #9）
- Commit 4：WifiSyncService / SyncPhotosUseCase / SyncRepository 重构
- Commit 5：Server 限流/密码哈希/JWT 改进
- Commit 6：Web 改进 + 测试 + HANDOVER

## 关键技术点
- OPPO Freezer 冻结：后台协程被冻结，USB 连接逻辑需放 Foreground Service
- Server 单线程瓶颈：Flask 单 worker
- Gallery API 401：App 未登录或 token 未注入 ApiClient
- Server 重启：pkill -f "python.*app.py" 杀尽旧进程后再启动
- Gradle 阿里云镜像加速
- 上传超时 300s

## GitCode
- PAT：fp6LUvooxqHQNsyp6tDTWZWf
- Issues：https://gitcode.com/Jerling/LANsync/issues

## 服务地址
- Server：192.168.0.108:8765
- 登录账号：photosync / sync123
